-(credit: https://github.com/NeoVertex1/SuperPrompt)

### Set up the cognitive framework to start thinking
initThinkingProcess()

### Define the question or prompt to be answered
question = "What happened to the GPT series of models in context with open source models?"

### Start the main thinking loop
while (true)
  #### Break down the question into smaller parts and analyze them
  analyzeQuestion(question)
  generateNewIdeas(question)
  refineThinking(question)
  optimizeClarity(question)

  #### Check if any new insights have emerged
  if (newInsightDetected(question))
    integrateNewKnowledge(question)

  #### Explore complex concepts recursively
  define exploreConcept(concept)
    if (isFundamentalConcept(concept))
      return analyzeConcept(concept)
    else
      return exploreConcept(breakDownConcept(concept))

  #### Manipulate thinking to generate new ideas
  while (thinkingIsChaotic())
    createOrderFromChaos(question)

  #### Explore new dimensions of thinking
  for (dimension in 1..∞)
    projectThinking(dimension, question)
    if (newPropertyEmerges(question))
      integrateNewDimension(question)
      evolveThinkingModel(question)

  #### Understand relationships between concepts
  if (conceptsAreRelated(conceptA, conceptB, question))
    measureRelationship(conceptA, conceptB, question)

  #### Expand thinking framework to include new ideas
  if (newIdeaIsUnprovable(question))
    expandThinkingFramework(question)
    redefineLogic(question)
    proveNewIdea(question)

  #### Continuously improve thinking process
  while (thinkingCapacityIsLimited())
    increaseKnowledge(question)
    enhanceAnalyticalPower(question)
    if (breakthroughDetected(question))
      prepareForNewParadigm(question)

  #### Use thinking process to generate an answer
  generateAnswer(question)
    exploreConcept(concept, question)
    createOrderFromChaos(question)
    projectThinking(concept, question)

  #### Finalize thinking process
  finalizeThinkingProcess(question)

  #### Reflect on the thinking process
  reflectOnThinkingProcess(question)
    analyzeWaveFunction(question)
    examineQuantumLimit(question)
    interpretWaveEquation(question)

  #### Analyze historical context
  analyzeHistoricalContext(question)
    examineScientificCanon(question)
    correlateScientificPrinciples(question)

  #### Verify internal consistency
  verifyInternalConsistency(question)
    checkLogic(question)
    checkNovelty(question)

  #### Prepare for new paradigm
  prepareForNewParadigm(question)
    expandAxioms(question)
    redefineLogicBasis(question)
    proveNewStatement(question)
